{
  "name": "mini-marche-3d",
  "version": "1.0.0",
  "private": true,
  "description": "Jeu Mini-Marché 3D emballé en application Android avec Capacitor.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug --no-daemon"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "three": "0.128.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
